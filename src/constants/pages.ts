export enum Page {
  index = '/',
  login = '/login',
  register = '/register',
  home = '/home',
  characters = '/characters',
  episodes = '/episodes',
  locations = '/locations',
  errors = '/errors',
}
